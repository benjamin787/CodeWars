const snail = aOfAs => {
    let answer = aOfAs[0];
    const n = aOfAs.length;
    for (let i = 0; i < n**2; i++) {

    }


    let answer = aOfAs.flat().map((x,i) => {
        if (i < aOfAs.length) {
            return x
        } else if ()
    })
    aOfAs.reduce((memo,row) => {
        memo.push(row[-1])
        return memo
    }, answer.slice(0,-1))
}